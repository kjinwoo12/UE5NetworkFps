# context-health

`.cursor` alwaysApply·스킬 비대 감사. `.cursor/skills/context-health/SKILL.md` 준수.

```
py .cursor/skills/context-health/scripts/audit_context.py
```

수정 반영은 `@self-update` / `/cursor-optimizer` 범위 안에서.
